# Authentication Server - Backend API

## How to run the project

Instlal the dependencies with your package manager of choice. For example, with `pnpm`:

```bash
pnpm install
```

Then, run the project with the following command:

```bash
pnpm dev
```
